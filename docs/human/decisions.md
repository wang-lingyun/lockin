# Decisions Log

Chronological record of product/architecture decisions. Newest at the bottom.
Significant architecture decisions also get a numbered ADR under `../adr/`.

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-14 | Adopt the requirements-first, staged, human-reviewed workflow. | Speed of vibe coding without losing control of product or repo quality. See [workflow-and-governance.md](workflow-and-governance.md). |
| 2026-06-14 | PRD v0.1 approved as canonical spec at `docs/specs/approved-prd.md`. | Single source of truth for all later work. |
| 2026-06-14 | Stack: Next.js + React + TS + Tailwind + Supabase, deployed on Vercel Hobby. | Free-tier friendly, AI-ready later. ADR 0001 / 0002. |
| 2026-06-14 | MVP auth = parent-only login + in-account student switcher. | Simplest path for a 1-parent / 2-student family; schema still designed for real student logins later. ADR 0004. |
| 2026-06-14 | Homework is text-first in MVP; uploads designed-for but deferred. | Vercel Hobby cost guardrails. ADR 0003. |
| 2026-06-14 | First pass scope = foundation only (governance scaffolding). No app code yet. | Establish docs/specs/rules before any code, per the workflow. |
| 2026-06-14 | PRD v0.2: generic subject **tracks** (sub-subjects) under any subject; Math = HMA/AoPS/Geometry/Calculus. | Math needs independently managed/scheduled sub-subjects; keep it generic. ADR 0005. |
| 2026-06-14 | Added **Chemistry** as a (bonus) subject with seed task. | Requested. |
| 2026-06-14 | Per-student **dated calendar** with times; missions derived on read (no cron). Supersedes the "no complex calendar" non-goal. | "Each track with its own schedule," kept free-tier safe. ADR 0006. |
| 2026-06-14 | **AI-native admin**: one typed Admin Command API + agent gateway (MCP/webhook) in MVP; Slack + voice connectors deferred to external modules. | Manage LockIn conversationally via agents; stay Vercel-Hobby friendly. ADR 0007. |
| 2026-06-14 | **Stage 1 vertical slice** built: `0001_init.sql` (schema + RLS + RPCs), shared zod command schemas, the command layer (`dispatch`→registry→handlers + `admin_command_log`), parent auth (login/signup/signout + middleware), parent dashboard (student switcher, today's missions, XP bar, admin forms). | First end-to-end path login→child→assign→complete→XP. UI and the future agent gateway share one validated command surface. |
| 2026-06-14 | **Stage 2 subjects & tracks** built (ADR 0005): `0002_subjects_tracks.sql` (`subject_tracks`, `student_subjects`, `student_subject_tracks` + RLS; Math tracks seeded; `subject_track_id` on tasks/missions), 4 new commands (`subject.create`, `track.create`, `student.setSubjectPriority/setTrackPriority`), `/settings` page for per-student three-way priority. | Per-student configurable subjects (AC 4/5/6); Math exposes HMA/AoPS/Geometry/Calculus (AC 33). Decided: UI = activate+prioritize only (create commands built, no custom-creation form yet); priority = primary/bonus/inactive, absence = inactive. |
| 2026-06-14 | **Stage 3 calendar & on-read missions** built (ADR 0006): `0003_schedule_blocks.sql` (`schedule_blocks` + RLS + the deferred `daily_missions.schedule_block_id` FK), full iCal **RRULE** recurrence via `rrule.js` (server-side `lib/missions/recurrence.ts`), `getTodaysMissions` merging persisted + virtual entries, 4 new commands (`schedule.block.create/update/delete`, `mission.completeScheduled`), `/schedule` week-agenda UI, dashboard now derived from the calendar. | Per-track dated schedule with times (AC 35) and Today's Missions derived on read with no cron (AC 36), within Vercel Hobby limits. Decided: a block materializes into a `daily_mission` only when marked done (idempotent via `(student,date,block)` unique key), so re-opening a day never duplicates; the add-block form exposes none/daily/weekly presets but stores real RRULE strings (agent can set arbitrary rules via the `update` command). |
| 2026-06-14 | **Stage 4 weekly goals (Quest Board)** built: `0004_weekly_goals.sql` (`weekly_goals` + RLS + `increment_weekly_goal` RPC), 4 new commands (`weeklyGoal.create/update/delete/incrementProgress`), `/quests` week-scoped board (`GoalCard` + progress bars + manual +/− / complete / archive / delete), dashboard "XP this week" + this-week goal progress. | Outcome-based weekly goals (AC 13) + dashboard weekly progress (AC 12), free-tier safe (read-time aggregates, no cron). Decided: goals are **week-scoped** and progress is **manual** in the MVP (no auto-derivation from missions); goals are **separate from XP** (completing a goal awards no XP); a goal **auto-completes** when current ≥ target; status enum = active/completed/archived; **no seed goals** (per-student and date-bound — parent creates them). |
| 2026-06-14 | Stage 4 added **no new ADR** — it implements PRD §10.6/§12 directly and reuses existing decisions (command surface ADR 0007, track attribution ADR 0005). | Not every stage is an architecture fork; a spec-driven feature on established patterns doesn't warrant an ADR. |
| 2026-06-14 | **Working agreement:** at any genuinely new architectural fork, Claude proposes a numbered ADR (Context / Decision / Status / Consequences) for human review **before** writing code; spec-driven features that reuse existing ADRs/patterns just get a decisions-log row. Next likely ADRs: homework file/metadata model (Stage 5, ADR 0003 lands here), agent-token auth & gateway transport (Stage 8, ADR 0007). | Keep durable rationale captured where it matters without ceremony on routine work. Reinforces the requirements-first, human-reviewed workflow. |
| 2026-06-14 | **Homework attachments via Supabase Storage** (ADR 0008, amends ADR 0003): private `homework` bucket, browser→Storage uploads via signed URLs (never through Vercel functions), Storage RLS via `owns_student()`, metadata-only in Postgres (`homework_submissions` + `homework_attachments`). Decided: images + PDF only (PNG/JPG/HEIC/PDF), **5 MB**/file, one submission = text and/or multiple attachments. Reserved nullable `url` column for a future Google Drive / external-link connector. | Parent wants real homework capture (photos of handwritten work, scanned PDFs) within Vercel-Hobby/Supabase-Free limits. Drive API rejected for MVP (OAuth + server hop); link-only deferred. |
| 2026-06-14 | **App timezone = Pacific** (`America/Los_Angeles`, DST-aware). `lib/date.ts` `todayISO()` now returns the Pacific calendar day, so today's missions, the Quest Board week, the schedule, and the weekly XP window all use the family's local day; the homework `submission_date` DB default is set to Pacific to match (`0006_…`). | The household is in Pacific; UTC `today` mis-dated late-evening activity by a day. Replaces the Stage-1/3 UTC-day assumption for a single-household MVP. Per-student timezones remain deferred (ADR 0006). |
| 2026-06-14 | **Stage 5 Homework Inbox** built (ADR 0008): `0005_homework.sql` (`homework_submissions` + `homework_attachments` + private `homework` Storage bucket + Storage RLS), 2 new commands (`homework.submit`, `homework.review`), `/homework` inbox (text + image/PDF submission via direct browser→Storage upload, paginated list with signed-URL previews, parent review panel), dashboard "to review" count + Homework link. Security review committed at `docs/specs/stage5-homework-security-review.md`. | Closes AC 14/15/16/29/30 (homework capture + review, AI-ready, file upload enabled) within free-tier limits. Decided: review_status = submitted/reviewed/needs_correction/mastered (PRD §10.8); `source_type` derived (pdf>photo>text); 5 MB/images+PDF enforced at client+schema+bucket; AI fields stored but never processed. |
| 2026-06-14 | **Stage 6 Mistake/Revision Bank + Reflections** built: `0007_mistakes_reflections.sql` (`mistake_bank_entries` + `reflections`, RLS, `updated_at` triggers, `reflections` unique per student/day, mistake→homework FK), 5 new commands (`mistake.create/update/delete`, `reflection.create/update`), `/mistakes` (entry form with optional homework link + status lifecycle) and `/reflections` (today's 4-prompt card create/edit + history with parent comments) pages, dashboard Mistakes/Reflections links + "to revisit" chip. | Closes AC 17/18/20 (create mistakes, link to homework, daily reflections). Spec-driven on established patterns (on-read derivation ADR 0006, command surface ADR 0007) → no new ADR. Decided: mistake_type is free text; reflections are one-per-day (editable, not duplicated); XP rewards for reflections/mistake-corrections deferred (mirrors Stage 4's "separate from XP"). |
| 2026-06-14 | **Streak now computed on read** (PRD §10.12): a day qualifies if the student wrote a reflection **OR** completed at least one mission; `lib/streak/computeStreak.ts` counts consecutive qualifying days ending today (today-in-progress doesn't break it), bounded to a 400-day window. The dashboard renders this; the stored `students.current_streak` column becomes a deprecated snapshot. | Lights up the long-deferred streak without a cron or a write-on-read (ADR 0006). The "reflection or mission" rule is the MVP's fixed "minimum daily requirement"; a per-student configurable threshold is deferred. |
| 2026-06-15 | **Stage 7 Coding Tracker + Rewards + manual XP + dashboard** built: `0008_coding_rewards.sql` (`coding_projects`, `coding_features`, `rewards` + RLS + `updated_at` triggers + RPCs `set_coding_feature_status`, `adjust_student_xp`), 11 new commands (`coding.project.*`, `coding.feature.*` incl. `setStatus`, `reward.*`, `xp.adjust`), `/coding` (project + feature-checklist UI) and `/rewards` (reward list with auto-unlock + XP-adjust control) pages, dashboard upgraded to PRD §10.1 (all-students glance strip, today's completion, latest reflection, next-reward progress) + Coding/Rewards links. | Closes AC 19 (coding projects) and advances §10.1/§10.12; spec-driven on established patterns (XP RPC like `complete_mission`, command surface ADR 0007, on-read derivation ADR 0006) → no new ADR. Decided: **completing a coding feature awards 20 XP**, idempotent via `coding_features.xp_awarded` guard + `xp_events` ledger (source_type `coding_feature`); **rewards auto-unlock on read** when `current_xp ≥ required_xp` (`rewardUnlocked` helper) — `rewards.unlocked`/`unlocked_at` columns reserved for a future manual/parent-approved path; **`xp.adjust`** writes the ledger (source_type `manual`) and clamps `current_xp ≥ 0`; coding-feature RLS flows through the parent project. |
| 2026-06-15 | **AI-ready tables deferred to a schema-only follow-up:** `personalized_learning_plans`, `ai_analysis_results`, `subject_rubrics` (+ a `plan.create` command) were scoped out of Stage 7 to keep it shippable, to be added as tables-only later (no UI, no inference). | AC 30/32 are satisfied by the structures **existing** and being extensible, not by Stage 7 building them now; splitting avoids an over-large stage. No AI inference runs on Vercel regardless (CLAUDE.md). |
| 2026-06-24 | **Stage 8A two-focus UI** built: one shared `AppHeader` (replaces 9 inline nav copies) encoding **Today** (calm, distraction-free daily missions — now the home `/`) vs **Manage** (`/manage`: all-students glance, admin forms, planning links). `withStudent()` nav helper + `StudentSwitcher` extracted. **Schedule editing** added (`EditBlockForm` + `updateBlockAction` → existing `schedule.block.update`). **Settings subject/track management** added: 2 new commands (`subject.update`, `track.update`) + UI to create/rename own subjects, add/rename their tracks, and hide/show tracks — defaults stay read-only (RLS `owner_parent_id = auth.uid()`). | Parent asked for a concise, two-focus product: no distractions on the daily page, one place to plan & configure (incl. adding Math + sub-categories). Spec-driven on established patterns (command surface ADR 0007, subjects/tracks ADR 0005, RLS already present) → **no new ADR, no migration**. Hengsheng's single account is both admin and student, so the split is two **modes**, not a parent-vs-kid login split (ADR 0004 holds). |
| 2026-06-24 | **Google Calendar sync split into ADR-gated Stage 8B** (not built): one-way ICS feed (LockIn → Google) at `app/api/calendar/[student]/route.ts`, HMAC-token auth via `CALENDAR_FEED_SECRET`, reusing the iCal RRULE already stored. | Reverses PRD §19 (external sync = MVP non-goal) and adds a **no-session, security-sensitive read path** — a genuine architectural fork. Per CLAUDE.md it needs ADR 0009 + a PRD §19 amendment + human approval before code; deferred out of 8A. |

| 2026-06-24 | **Stage 9 target finish time** built: `0009_schedule_block_estimated_minutes.sql` adds nullable `schedule_blocks.estimated_minutes` (mirrors `tasks.estimated_minutes`); `estimatedMinutes` added to `ScheduleBlockCreate/UpdateInput` + handlers; a "Target (h)" input (min 0.5, step 0.5) on the task + add/edit-block forms (Manage), converted hours→minutes in the server actions. **Today** now shows the long date (e.g. "June 22, 2026"), each mission's target (`hoursLabel`, e.g. "1.5h"), and a summed "~Xh planned" total. | Parent wants Today to show the date and how long each item should take, in hours at 0.5h granularity. A planning **estimate**, not time-tracking, so it's consistent with the outcome-based principle — and `estimated_minutes` already existed for tasks. Spec-driven on established patterns (command surface ADR 0007) → **no new ADR**; the additive, non-RLS column is reflected in PRD §22/§301-303. |
| 2026-06-25 | **Tasks can be attributed to a track at creation.** Added an optional `subjectTrackId` to `TaskCreateInput` (+ handler writes `tasks.subject_track_id`, column already existed since 0002) and a dependent **Track** picker to the Create-a-task form (options filtered by the chosen subject; cleared when the subject changes). `createTaskAction` now also `revalidatePath("/manage")` so a new task appears immediately on Manage. Lets parents create per-track Math work (HMA / AoPS / AMC 10 / …) that the active-track-scoped assign list then surfaces. | Turning on tracks alone never produced assignable work — the create form was subject-only, so no task could belong to AoPS/AMC 10/etc. Additive, reuses ADR 0005 + the command surface (ADR 0007); no migration. |
| 2026-06-25 | **"Assign a task" list scoped to the student's active subjects & tracks.** `app/manage/page.tsx` previously fetched all tasks globally; it now offers a task only if its most-specific attribution is on for the selected student — `subject_track_id` active, else `subject_id` active, else (unattributed) always offered. Active = `student_subjects`/`student_subject_tracks` `priority_type != 'inactive'` (absence = inactive), the same rule Settings uses. Empty-state copy now points to Settings. | Parent wants the assignable tasks driven by what's turned on per student, not a flat global list. Read-time, student-scoped; reuses activation (ADR 0005) + command/derivation patterns → no migration, no new ADR. |
| 2026-06-25 | **Manage page decluttered to glance + task forms.** Removed the Schedule / Settings / Quests / Homework / Coding / Mistakes links from the Manage secondary nav and the planning entry-point cards, plus the active-student "This week's goals" and "Latest reflection" summary blocks and the "Add a student" panel. The pages stay reachable by direct URL (AppHeader keeps `MANAGE_PAGES` for mode detection / switcher return path; `MANAGE_LINKS` is now empty). Also renamed student "Hengsheen" → "Hengsheen Wang" (data-only). | Parent wants Manage to be a focused surface (all-students glance + assign/create task) without the feature-link clutter; features hidden, not deleted, so they can be re-surfaced later. No migration / no new ADR. |
| 2026-06-25 | **XP, Levels, and Rewards removed; Streak kept; "unmark done" added** (ADR 0010, amends PRD §10.12 + AC §16(11)(12), retires the Reward model §16.16 and the Rewards/XP page §13.12). Migration `0010_remove_xp_keep_streak.sql` is **non-destructive**: `create or replace` makes `complete_mission` and `set_coding_feature_status` XP-free and adds `uncomplete_mission`; all XP columns/tables (`students.current_xp/current_level`, `tasks.xp_value`, `daily_missions/coding_features.xp_awarded`, `rewards`, `xp_events`, `level_for_xp`/`adjust_student_xp`) are left **dormant, not dropped**. Shared package drops the XP/reward/level exports + `xpValue`, adds `mission.uncomplete`; `/rewards` page + `XpBar` deleted; Today gains an **Undo** button next to "✓ Done". | Parent (also the sole student) wants streak-only motivation — points/levels are the wrong framing here. Streak was already XP-independent (`computeStreak`), so removal is safe. DROPs avoided because one Supabase instance is also production; a later reviewed migration can drop the dormant schema. Rewards removed (not repurposed) since it only worked as an XP-threshold unlock. |
| 2026-06-24 | **Stage 10 Today focus refinements** built (no migration, no new ADR): **(1)** Today's headline metric switches from the global Level/XP bar to a **"% done today"** bar (`missionsDone / missions.length`); Level/XP stays everywhere else (Manage, Rewards). **(2)** **Per-task reflection** — new `mission.setReflection` command + `MissionSetReflectionInput` (zod, `missionId` OR `studentId`+`scheduleBlockId`+`date`); handler reuses the `missionCompleteScheduled` upsert to materialize a virtual block before writing `daily_missions.student_reflection` (column already existed since 0001). Compact `MissionReflection` client component under each mission. The whole-day Reflections page is unchanged. **(3)** Nav: `homework`/`coding`/`mistakes` moved from `TODAY_LINKS` to `MANAGE_LINKS`; Today keeps only Reflections. **(4)** "Tomorrow's schedule →" link on Today via new pure `nextISODate()` (passes `?week=` to `/schedule`). | Parent wants Today to be a tighter daily focus: a per-day progress signal instead of a long-horizon level, a place to reflect on each task (not just the day), fewer admin links on the calm page, and a one-tap jump to plan tomorrow. Additive on existing patterns (command surface ADR 0007, on-read derivation ADR 0006); `student_reflection` already existed → **no migration, no new ADR**. PRD §10.1/§10.8/§10.12. |
| 2026-06-25 | **Settings "Subjects & tracks" refreshed + delete for owned items.** Added `subject.delete` / `track.delete` commands (+ `SubjectDeleteInput`/`TrackDeleteInput` zod, handlers, registry, `deleteSubject/TrackAction` server actions) so a parent can delete subjects/tracks **they own**; built-ins stay rename/delete-proof (RLS `owner_parent_id = auth.uid()`). New `OwnedItemControls` client component gives owned items a view→**Edit**→**confirm-Delete** flow (supersedes the always-open `InlineRename`, now removed); built-ins render plain text + a muted **"Built-in"** badge. `AddTrackForm` collapses behind a "+ Add sub-category" toggle. The list now shows the parent's own subjects + the student's **active** built-ins, with **Off** built-ins tucked into a collapsed `<details>` "Off subjects (N)". Delete/rename/create-track actions also `revalidatePath("/manage")` so the Create/Assign-task surfaces refresh. | Parent asked for a cleaner, more consistent Settings UI and for owned subjects to be deletable (not just renamable). Safe with no migration: `subject_tracks.subject_id` is `ON DELETE CASCADE` and `tasks`/`daily_missions` track FKs are `ON DELETE SET NULL`, while RLS already gates DELETE to owners. Reuses ADR 0005 (per-student activation) + command surface (ADR 0007) → **no new ADR**. |
| 2026-06-25 | **Tasks are listable, renamable & deletable from Manage.** Added `task.update` / `task.delete` commands (+ `TaskUpdateInput` partial / `TaskDeleteInput` zod, handlers, registry, `updateTask/deleteTaskAction` server actions) and a **"Tasks"** panel on `/manage` (new `TaskList` client component: view→Edit-rename→confirm-Delete per row, labelled by track/subject). The Manage `AppHeader` also gained a "← Manage" back link on sub-pages so Settings/Schedule are easy to leave. Previously the only task management was create + assign, leaving a stray, mis-named "AOPS" task with no way to fix or remove it. | Parent needs to clean up / rename tasks (the lone "AOPS") and manage them going forward, rather than a one-off prod DB write. Safe with no migration: `tasks` RLS `"owner all"` gates UPDATE+DELETE on `created_by = auth.uid()` (set at create), `task_assignments.task_id` is `ON DELETE CASCADE` and `daily_missions.task_id` is `ON DELETE SET NULL` (history preserved). Reuses the command surface (ADR 0007) → **no new ADR**. |
| 2026-06-25 | **Deleting a task now clears its pending missions; missions are removable from Today; block-derived missions keep their title.** (1) `taskDelete` first deletes the task's non-completed `daily_missions` (completed ones stay for streak/history) — otherwise the `ON DELETE SET NULL` FK left title-less "Untitled task" orphans on a day. (2) New `mission.delete` command (+ `MissionDeleteInput`, handler, registry, `deleteMissionAction`) and a **Remove** button on every persisted Today mission (virtual schedule-block items have none — edit the block instead). (3) `getTodaysMissions` falls back to the originating block's title for missions materialized from a task-less schedule block. | Parent deleted the "AOPS" task and its already-assigned mission became an "Untitled task" on Today (subject survived, title gone). The completed-vs-pending split preserves history while removing meaningless leftovers; Remove gives manual control over any unwanted/orphan mission. Safe with no migration; reuses `daily_missions` RLS (`owns_student`) + command surface (ADR 0007) → **no new ADR**. |

## Open decisions / to revisit

- Exact RLS keying strategy once real student logins are introduced.
- Whether the daily-streak recalculation is on-demand vs a once/day job.
- Hosting region / Supabase project provisioning details (pre-production).
- **"HMA" meaning** — assumed a renameable Math-track label; confirm if it maps to a
  specific program.
- **Agent gateway transport** — MCP server vs. signed webhooks vs. both (ADR 0007).
- **[Default applied, confirm] Subject membership source of truth** = `StudentSubject`
  (+ `StudentSubjectTrack`); `LearningProfile` arrays removed (PRD §12).
- **[Default applied, confirm] Missions model** = `ScheduleBlock` is the plan,
  `DailyMission` created lazily on read with `schedule_block_id`, nullable `task_id`
  (PRD §12). Alternative: keep calendar and missions fully separate.
- **Stage 0 npm audit:** 7 advisories remain (esbuild via vitest dev server; postcss
  bundled in Next). All dev/transitive, not reachable in the deployed static app; fixes
  require breaking major bumps. Deferred — revisit when bumping Next/vitest majors.
- ~~**Streak recalculation (Stage 1):** `students.current_streak` exists and is displayed
  but is not yet updated (defaults 0).~~ **Resolved (Stage 6):** computed on read in
  `lib/streak/computeStreak.ts` (qualifying day = reflection or completed mission); the
  stored column is now a deprecated snapshot.
- **Middleware Edge-runtime warning:** `@supabase/ssr` triggers a benign `process.version`
  warning in the Edge middleware build. Non-fatal, build passes; standard for this stack.
- **Stage 1 ad-hoc missions:** `daily_missions.schedule_block_id` is null in Stage 1, so
  the `unique(student_id, date, schedule_block_id)` constraint permits multiple ad-hoc
  missions per day (NULLs are distinct). Schedule-block FK + dedup arrive in Stage 3.
- **Calendar timezone (Stage 3 / updated 2026-06-14):** "today" is now **Pacific**
  (`lib/date.ts` `todayISO()`), but schedule block **times** (`start_at`/`end_at`) and the
  weekly XP window's instant boundary are still computed/displayed in **UTC**. So the
  current day is correct for a Pacific household, but a block's clock time may read shifted.
  Full per-instant timezone correctness (and per-student timezones) is still deferred; the
  schema stores `timestamptz`, so only read-time conversion is needed later.
- ~~**Streak recalculation (Stage 3):** still not implemented — `current_streak` stays a
  displayed default.~~ **Resolved (Stage 6):** see above — computed on read.
- **Streak threshold configurability (Stage 6):** the qualifying rule is fixed
  (reflection OR ≥1 completed mission). PRD §10.12 envisions a "configurable minimum
  daily requirement" per student; deferred until there's a settings surface for it.
- **XP for reflections / mistake-corrections (Stage 6):** PRD §10.12 lists reflection = 5
  XP and mistake correction = 10 XP, but Stage 6 does **not** award XP for either (no
  `xp_events` writes). Mirrors Stage 4's "goals are separate from XP." Revisit alongside
  the gamification/analytics pass; would also reopen edit/un-master re-award semantics.
- **Weekly goal auto-progress (Stage 4):** goal `current_value` is bumped manually
  (`increment_weekly_goal`). Auto-deriving progress from completed missions/homework (e.g.
  "+1 per AoPS mission") is deferred — would link `weekly_goals` to a metric source; revisit
  with the analytics/AI work. Goals are also intentionally **separate from XP** for now.
- **AI-ready tables (Stage 7, deferred):** `personalized_learning_plans`,
  `ai_analysis_results`, `subject_rubrics` are not yet created. Planned as a schema-only
  follow-up migration (tables + RLS, no UI, no inference) to satisfy AC 30/32 by being
  present and extensible. Revisit when the AI-analysis pass begins.
- **Manual reward unlock (Stage 7, reserved):** `rewards.unlocked` / `unlocked_at` columns
  exist but are unused — MVP derives unlock on read (`current_xp ≥ required_xp`). A
  parent-approved / manually-granted unlock path (e.g. non-XP rewards, or revoking an
  XP-unlocked one) would write these columns; deferred until there's a need.
- **Coding-feature XP un-award (Stage 7):** completing a feature awards 20 XP once
  (idempotent); moving it back to in-progress/not-started does **not** claw the XP back
  (`xp_awarded` stays set, so re-completing won't re-award). Matches the mission model.
  Revisit if XP accuracy on un-completion matters.
- **Orphaned homework Storage objects (Stage 5):** files upload browser→Storage before the
  `homework.submit` row is written; if submit fails or the user leaves, the objects linger
  (still RLS-private, count against the family's own quota). No cleanup sweep — no cron on
  the free tier. Accepted MVP tradeoff; revisit if cost/clutter becomes real. See
  `docs/specs/stage5-homework-security-review.md`.
